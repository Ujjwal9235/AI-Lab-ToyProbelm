tb=int(input('bananas at starting='))
d=int(input('distance to be covered='))
load_capacity=int(input('max load capacity of camel='))
lb=0
start=tb
for i in range(d):
    while start>0:
        start=start-load_capacity
#Here if condition is checking that camel doesn't move back if there is only one banana left.
        if start==1:
            lb=lb-1#Lose is decreased because if camel try to get remaining one banana he will lose one extra banana for covering that two miles.
#Here we are increasing lose because for moving backward and forward by one mile two bananas will be lose
        lb=lb+2
#Here lose is decreased as in last trip camel will not go back.
    lb=lb-1
    start=tb-lb
    if start==0:#Condition to check whether it is possible to take a single banana or not.
        break
print(start)
